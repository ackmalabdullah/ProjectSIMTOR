name: mobile_app
description: "Aplikasi Simulasi Kredit Motor Honda"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  firebase_core: ^2.24.0
  firebase_auth: ^4.13.0
  google_sign_in: ^6.2.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
